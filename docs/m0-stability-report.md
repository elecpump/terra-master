# M0 30 分钟运行稳定性验收

日期：2026-09-11（Asia/Shanghai）。TerraBridge 0.4.3，protocol 1，realtime。

## 结论

单实例前后台 30 分钟运行连续性通过。此次没有发送动作或 reset、没有重启游戏；先后台后前台，切换期间采样持续。死亡状态如实保留，结论不等于无死亡训练场景、完整 reset 或训练就绪。

| 指标 | 实测 |
|---|---|
| 总时长 | 1800.051 秒 |
| 样本数 | 1723 |
| 后台覆盖 | 950.193 秒，约 15 分 50 秒 |
| 前台覆盖 | 847.635 秒，约 14 分 8 秒 |
| 世界 tick 增量 | 107998，约 59.998 tick/s |
| 最大采样间隔 | 1.235 秒 |
| 峰值工作集 | 956985344 字节，约 913 MiB |
| 峰值 private bytes | 1774538752 字节，约 1.65 GiB |
| 死亡状态样本 | 660；不是死亡次数 |
| 实例/世界切换、陈旧快照、tick 停滞 | 均未发现 |

目标前后台各 15 分钟；本轮预登记最低判定为总计至少 1800 秒、每种焦点至少 600 秒、无运行异常或超过 5 秒的采样间隔。焦点按相邻样本端点估算，切换间隔不归入任意一侧，不能当作毫秒级连续窗口追踪。焦点来自 Windows 前台进程信息，本轮引擎焦点与系统焦点无不一致样本。

## 证据与复跑

- [判定与原始报告 SHA-256](../evidence/stability-v043-summary.json)
- [配置及采样器、运行清单哈希](../evidence/stability-v043-config.json)
- 完整逐秒记录：`runs/stability-v043/report.json`，3133272 字节，按项目规则不纳入 Git。SHA-256 为 `10e56f7063eeb764804325f76e4187414b6595780e27fc8418e7a9adb0823e98`。

采样器当前 SHA-256 与配置中记录一致。`measure_runtime.py --progress-output` 提供每约 30 秒的进展；`summarize_stability.py` 单独检查时长、双焦点覆盖和采样空档，保留来源哈希。复跑命令见 [训练指南](training-profile.md)。新增判定测试覆盖单焦点、时长不足、报告失败、焦点缺失和首段/中段采样缺口；全套 28 项离线测试通过。

## 后续边界

M0 的 30 分钟运行连续性缺口已关闭，结合此前前后台动作、reset、暂停超时与依赖 smoke 证据，可进入 D1 同步推进实验。此次不再将自然世界的死亡与进程运行连续性混为一个结论：死亡和安全场景属于后续环境验收，trainingReady 仍为 false，未开始 Terraria 训练。

长测期间完成了只读安装版 IL 勘察，见 [D1 准备记录](d1-loop-investigation.md)。没有修改运行中的游戏循环，尚无同步支持或确定性结论。
